<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinUniverse.Mobile.Views.NewsOverviewPage"
                 xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:converters="clr-namespace:XamarinUniverse.Mobile.Converters;assembly=XamarinUniverse.Mobile"
             xmlns:controls="clr-namespace:XamarinUniverse.Mobile.Controls;assembly=XamarinUniverse.Mobile"
             xmlns:extensions="clr-namespace:XamarinUniverse.Mobile.Extensions;assembly=XamarinUniverse.Mobile"
             xmlns:behaviors1="clr-namespace:XamarinUniverse.Mobile.Behaviors"
             Title="News"
             >

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:FeaturedImageConverter x:Key="FeaturedImageConverter"/>
            <converters:HtmlConverter x:Key="HtmlConverter"/>
            <converters:ImageResourceConverter x:Key="ImageResourceConverter"/>
            <converters:SelectedItemConverter x:Key="SelectedItemConverter"/>

            <x:String x:Key="PlaceholderImage">XamarinUniverse.Mobile.Assets.placeholder.jpg</x:String>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid>
            <controls:IncrementalListView ItemsSource="{Binding Posts}"
                                          SeparatorVisibility="None"
                                          HasUnevenRows="True"
                                          IsPullToRefreshEnabled="True"                  
                                          RefreshCommand="{Binding LoadPostsAsyncCommand}"
                                          IsRefreshing="{Binding IsRefreshing, Mode=OneWay}">
                <controls:IncrementalListView.Behaviors>
                    <behaviors1:EventToCommandBehavior EventName="ItemSelected" Command="{Binding SelectPostCommand}" Converter="{StaticResource SelectedItemConverter}" />
                </controls:IncrementalListView.Behaviors>
                <controls:IncrementalListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Frame BackgroundColor="White" Margin="10,5">
                                <Grid Margin="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <forms:CachedImage HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
                                                   HeightRequest="200"         
                                                   DownsampleToViewSize="true"
                                                   Source = "{Binding Embedded, Converter={StaticResource FeaturedImageConverter}}"
                                                   Aspect="AspectFill"
                                                   LoadingPlaceholder="{Binding ., Source={StaticResource PlaceholderImage}, Converter={StaticResource ImageResourceConverter}}"
                                                   FadeAnimationEnabled="True">
                                    </forms:CachedImage>

                                    <Label Text="{Binding Title.Rendered, Converter={StaticResource HtmlConverter}}"
                                       FontSize="Medium" FontAttributes="Bold" Grid.Row="1" />

                                </Grid>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </controls:IncrementalListView.ItemTemplate>
            </controls:IncrementalListView>

            <Label Text="{extensions:Translate NewsOverviewPageNoPostsLabel}"
                   IsVisible="{Binding ArePostsNotAvailable}"
                   VerticalTextAlignment="Center"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   Margin="12"
                   FontAttributes="Italic"/>

        </Grid>
    </ContentPage.Content>
</ContentPage>